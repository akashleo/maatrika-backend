// Test setup file - runs before all tests
// Add any global test configuration or mocks here

// Silence console logs during tests (optional, comment out if you want to see logs)
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };

// Set default test environment variables
process.env.NODE_ENV = 'test';
